<Window x:Class="Typing.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Typing.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Background="#add5ff">
    <Window.DataContext>
        <vm:MainVM/>
    </Window.DataContext>

    <Grid>
        <StackPanel>
            <ListView ItemsSource="{Binding Words}" Width="350" Height="211" Background="White" BorderThickness="1"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled" Margin="0 30 0 0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Word}" Margin="0 0 -5 -1" Background="{Binding Color}" Padding="3 0 3 3"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
            </ListView>
            <!--<Border BorderThickness="1" BorderBrush="Black" Width="300" Height="35" VerticalAlignment="Top" Margin="0 30 0 0">
                <TextBlock Text="{Binding TextBlock, UpdateSourceTrigger=PropertyChanged}"
                   Width="300" Height="35" TextWrapping="Wrap" Background="White"/>
            </Border>-->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 10 0 0">
                <TextBox Text="{Binding Texted, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" HorizontalAlignment="Center" Width="200"/>
                <TextBlock Text="{Binding Timer}" Background="#3c4d5c" Foreground="White" Margin="5 0 0 0" Height="20" Width="30" TextAlignment="Center" Padding="0 1 0 0"/>
                <Button Content="🔄️" Background="#428bca" Foreground="White" Margin="5 0 0 0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
